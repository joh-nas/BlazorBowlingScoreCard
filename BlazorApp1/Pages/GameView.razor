@page "/gameview"
@inherits Game;
@using BlazorApp1.Classes;
<h3>Bowling Score Board</h3>
@for (int i = 0; i < @Players.Length; i++)
{
    <GameScoreView PlayerName="@Players[i]"></GameScoreView>
}

<button class="shotResultBtn" @onclick="() => AddPlayer()">+</button>

@code
{
}
